package com.example.shapeshifter;
import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
// Class definition for NextActivity
public class NextActivity extends AppCompatActivity {
    // Method called when the activity is first created
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState); // Call the superclass onCreate() method
        setContentView(R.layout.activity_next); // Set the layout for the activity using the activity_next.xml layout file
// Any additional initialization or logic can go here
    }
}
